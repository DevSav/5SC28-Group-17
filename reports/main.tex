\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}

% Report requirements from the announcement:
% - IEEE two-column format.
% - Final PDF should be 6--12 pages.
% Suggested page split:
% - Introduction and evaluation setup: 1 page.
% - ANN modelling: 2--3 pages.
% - GP modelling: 1.5--2 pages.
% - Policy learning and reference tracking: 2--3 pages.
% - Real setup, discussion, and conclusion: 1--2 pages.

\title{Machine Learning for Modelling and Control of an Unbalanced Disk\\
Group 17 Design Project}

\author{\IEEEauthorblockN{TODO Student 1 Name, TODO Student 2 Name, TODO Student 3 Name}
\IEEEauthorblockA{\textit{5SC28 Machine Learning for Systems and Control}\\
\textit{Eindhoven University of Technology}\\
TODO student numbers}
}

\begin{document}

\maketitle

\begin{abstract}
This report presents the Group 17 design project for the modelling and control of an unbalanced disk.
The project combines system identification with policy learning.
The system dynamics are modelled using Artificial Neural Network (ANN) and Gaussian Process (GP) models, using motor voltage as input and measured disk angle as output.
For the ANN part, a simple NARX network is compared with recurrent GRU and LSTM networks, while the GP part uses a sparse delta-output GP-NARX model.
The best ANN prediction result is obtained with the GRU, the best ANN simulation result is obtained with the LSTM, and the final GP gives the lowest modelling errors among the tested configurations.
\end{abstract}

\begin{IEEEkeywords}
unbalanced disk, system identification, Artificial Neural Network, Gaussian Process, NARX, GP-NARX, GRU, LSTM, prediction, simulation
\end{IEEEkeywords}

\section{Introduction}

\subsection{Project Motivation}

The unbalanced disk is a nonlinear dynamical system where a motor rotates a disk with an uneven mass distribution.
Because gravity, friction, inertia, and motor voltage all influence the motion, the setup is useful for testing machine learning methods for modelling and control.
The aim of the project is to apply the methods from 5SC28 in a systematic way: choose model structures, tune them, validate them, and discuss their limitations \cite{assignment5sc28}.

\subsection{Assignment Objectives}

The assignment has three main objectives \cite{assignment5sc28}.
First, the system dynamics must be identified using both an Artificial Neural Network (ANN) model and a Gaussian Process (GP) model.
Second, a policy must be learned that can swing the disk up from the stable bottom position to the upright position.
Third, the controller must be extended so that one policy can swing up the disk and track a reference around the upright position.

For the modelling part, the input is the applied motor voltage \(u(k)\) and the output is the measured disk angle \(\theta(k)\).
Angular velocity was not used, following the assignment requirement \cite{assignment5sc28}.
Fig.~\ref{fig:system_overview} shows the input-output structure used for the modelling task.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\columnwidth]{figures/system_overview.png}}
\caption{Input-output view of the unbalanced disk system. The applied motor voltage \(u(k)\) is used as model input, and the measured disk angle \(\theta(k)\) is used as model output.}
\label{fig:system_overview}
\end{figure}

\subsection{Prediction and Simulation}

The models are evaluated using both prediction and simulation.
In prediction, the model uses measured past angles to predict the next angle.
In simulation, the model feeds back its own previous predictions to generate a full trajectory.
Simulation is harder because errors can accumulate over time, so both tasks are needed to judge the model properly \cite{ljung1999system}.
The error was evaluated using the root mean squared error (RMSE):

\begin{equation}
\mathrm{RMSE} =
\sqrt{\frac{1}{N}\sum_{k=1}^{N}\left(\theta(k)-\hat{\theta}(k)\right)^2},
\end{equation}

where \(\theta(k)\) is the measured disk angle, \(\hat{\theta}(k)\) is the model output, and \(N\) is the number of evaluated samples.

\section{Artificial Neural Network Modelling}
\input{ANN_part}

\section{GP Modelling Method}
\input{GP_part}

\section{Policy Learning}

This section should be completed by the control group member.
It should describe the learned policies used to swing up the disk and keep it near the upright position.

\subsection{Swing-Up Objective}

TODO: Explain the control goal.
The controller should move the disk from the stable bottom position to the unstable upright position and keep it there.
Mention the voltage limit and how success is measured.

\subsection{Q-Learning or DQN Controller}

TODO: Explain the Q-learning/DQN controller.
Include the state, action, reward function, exploration method, training setup, and final simulation result.
Useful theory points are the Markov decision process, Q-function, reward, and exploration-exploitation trade-off.
If a DQN was used, cite the DQN reference.

\subsection{Model Internalization or Actor-Critic Controller}

TODO: Explain the second control method.
Describe how it differs from the Q-learning/DQN method and why it was tested.
If an actor-critic or policy-gradient idea was used, cite the policy-gradient reference.

\subsection{Swing-Up Results}

TODO: Add a table with the final swing-up results.
Useful metrics are success rate, mean absolute angle error near upright, and control effort.
Also include one angle plot and one voltage plot if space allows.

\section{Reference Tracking}

TODO: Explain how the controller was extended from swing-up to reference tracking.
The report should describe the reference signal, the reward function, and how one policy handles both swing-up and tracking.

\subsection{Reference Tracking Results}

TODO: Add the final tracking result table.
Useful metrics are tracking RMSE, success rate, and tracking ratio.
Include a plot with the reference angle and the measured/simulated disk angle.

\section{Real Setup Experiments}

This section should be filled in after the real setup tests.
If the tests are not successful, still describe what happened and why.
The discussion of practical limitations is useful for the final grade.

\subsection{Test Plan}

TODO: Record the date, controller version, number of trials, initial condition, safety checks, and logged signals.

\subsection{Experimental Results}

TODO: Add the real setup results.
Compare the real setup behaviour with the simulation behaviour.
Mention possible causes of differences, such as friction, sensor noise, delay, saturation, or model mismatch.

\section{Discussion}

This section should compare the main choices made in the project.
It should not only repeat the results, but also explain what was learned from them.

\subsection{Model Comparison}

Table~\ref{tab:ann_gp_comparison} compares the best ANN results with the final GP result.
For the ANN, the best prediction result was obtained with the GRU model, while the best simulation result was obtained with the LSTM model.
The GP model was a sparse delta-output GP-NARX model.

\begin{table}[htbp]
\caption{Best ANN and GP modelling results. The ANN values are the best ANN results per task on the held-out test split, while the GP values are validation results from the GP pipeline.}
\centering
\begin{tabular}{lcc}
\toprule
Model & Prediction RMSE & Simulation RMSE \\
\midrule
Best ANN & 0.202 deg & 1.893 deg \\
Best GP & 0.164 deg & 1.461 deg \\
\bottomrule
\end{tabular}
\label{tab:ann_gp_comparison}
\end{table}

Fig.~\ref{fig:ann_gp_comparison} shows the same comparison visually.
The GP model obtained the lowest recorded prediction and simulation
RMSE. This suggests that the sparse delta-output GP-NARX structure
captured the system dynamics slightly better than the tested ANN
architectures. The ANN comparison remains useful because it shows the
improvement from a simple NARX baseline to recurrent models, and because
neural networks can be extended flexibly when more data or larger
architectures are available.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\columnwidth]{figures/ann_gp_model_comparison.png}}
\caption{Comparison of the best recorded ANN and GP modelling errors.}
\label{fig:ann_gp_comparison}
\end{figure}

It should be noted that the two values reported for the ``Best ANN''
represent the best results obtained by two different recurrent models.
The prediction value was obtained with the GRU, whereas the simulation
value was obtained with the LSTM. The table therefore compares the best
ANN result for each modelling task with the corresponding result of the
final GP model.

Relative to the best ANN results, the GP reduced prediction RMSE by
about \(19\%\) and simulation RMSE by about \(23\%\).
The improvement in simulation is particularly relevant because free-run
simulation tests whether the identified model can reproduce the system
dynamics recursively, without receiving the measured angle at every
time step.

The results indicate that the selected GP structure was well suited to
the amount and type of identification data available in this project.
The GP also provides a predictive variance in addition to the predicted
mean. This gives an indication of model uncertainty, which may be useful
for model validation or uncertainty-aware control. However, the present
comparison is based only on RMSE, so this probabilistic information was
not included directly in the performance score.

The ANN and GP approaches also have different practical advantages.
The GP provides a probabilistic framework and achieved high accuracy
with the available dataset, while recurrent neural networks can be
scaled more easily to substantially larger datasets and more complex
architectures. Therefore, the lower GP errors in this project should not
be interpreted as a general conclusion that GP models are always more
accurate than ANN models. They only show that the final sparse GP model
performed best among the configurations tested here.

A strictly fair numerical comparison would require the ANN and GP
models to use exactly the same held-out samples, preprocessing steps,
simulation initialization, simulation horizon, and error definition.
In this project, the ANN values were computed on the ANN held-out test
split, while the GP values were reported from the GP validation
pipeline. The comparison in Table~\ref{tab:ann_gp_comparison} should
therefore be interpreted as a practical comparison of the final
recorded models, not as a perfectly controlled benchmark.



\subsection{Controller Comparison}

TODO: Compare the control methods.
Discuss robustness, tuning difficulty, success rate, and simulation-to-real transfer.

\subsection{Limitations}

The main project limitation is that the search over models and
controllers was limited by time. The selected ANN, GP, and control
methods are therefore the best among the configurations tested in this
project, but they are not guaranteed to be globally optimal.

The conclusions also depend on the available data and simulation
conditions. The modelling data only cover a finite range of voltage
signals and disk motions, so performance may change for trajectories
outside this range. Similarly, policies that work well in simulation
may behave differently on the real setup because of friction, delay,
sensor noise, saturation, or mismatch between the simulator and the
physical system.

The comparison between methods should also be interpreted carefully.
The ANN and GP models were both tested on prediction and simulation
tasks, but they were not tuned with exactly the same number of trials or
the same type of hyperparameters. The true measured outputs for the
hidden benchmark tests were not available during development, so the
final hidden-test files could only be checked for the required format
before being evaluated by the course staff.

\section{Conclusion}

TODO: Summarize the final project results in one short section.
Mention the best modelling result, the best control result, and the outcome of the real setup test if available.
Also state what the group would improve with more time.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
