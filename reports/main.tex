\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}

% Report requirements from the announcement:
% - IEEE two-column format.
% - Final PDF should be 6--12 pages.
% Suggested page split:
% - Introduction and evaluation setup: 1 page.
% - GP modelling: 1.5--2 pages.
% - ANN modelling: 2--3 pages.
% - Policy learning and reference tracking: 2--3 pages.
% - Real setup, discussion, and conclusion: 1--2 pages.

\title{Machine Learning for Modelling and Control of an Unbalanced Disk\\
Group 17 Design Project}

\author{\IEEEauthorblockN{TODO Student 1 Name, TODO Student 2 Name, TODO Student 3 Name}
\IEEEauthorblockA{\textit{5SC28 Machine Learning for Systems and Control}\\
\textit{Eindhoven University of Technology}\\
TODO student numbers}
}

\begin{document}

\maketitle

\begin{abstract}
This report presents the Group 17 design project for the modelling and control of an unbalanced disk.
The project combines system identification with policy learning.
The system dynamics are modelled using Gaussian Process (GP) and Artificial Neural Network (ANN) models, using motor voltage as input and measured disk angle as output.
For the ANN part, a simple NARX network is compared with recurrent GRU and LSTM networks.
The GRU gives the best one-step prediction result, while the LSTM gives the best simulation result.
\end{abstract}

\begin{IEEEkeywords}
unbalanced disk, system identification, Artificial Neural Network, NARX, GRU, LSTM, prediction, simulation
\end{IEEEkeywords}

\section{Introduction}

\subsection{Project Motivation}

The unbalanced disk is a nonlinear dynamical system where a motor rotates a disk with an uneven mass distribution.
Because gravity, friction, inertia, and motor voltage all influence the motion, the setup is useful for testing machine learning methods for modelling and control.
The aim of the project is to apply the methods from 5SC28 in a systematic way: choose model structures, tune them, validate them, and discuss their limitations \cite{assignment5sc28}.

\subsection{Assignment Objectives}

The assignment has three main objectives \cite{assignment5sc28}.
First, the system dynamics must be identified using both a Gaussian Process (GP) model and an Artificial Neural Network (ANN) model.
Second, a policy must be learned that can swing the disk up from the stable bottom position to the upright position.
Third, the controller must be extended so that one policy can swing up the disk and track a reference around the upright position.

For the modelling part, the input is the applied motor voltage $u(k)$ and the output is the measured disk angle $\theta(k)$.
Angular velocity was not used, following the assignment requirement \cite{assignment5sc28}.

\subsection{Prediction and Simulation}

The models are evaluated using both prediction and simulation.
In prediction, the model uses measured past angles to predict the next angle.
In simulation, the model feeds back its own previous predictions to generate a full trajectory.
Simulation is harder because errors can accumulate over time, so both tasks are needed to judge the model properly \cite{ljung1999system}.
The error was evaluated using the root mean squared error (RMSE):

\begin{equation}
\mathrm{RMSE} =
\sqrt{\frac{1}{N}\sum_{k=1}^{N}\left(\theta(k)-\hat{\theta}(k)\right)^2},
\end{equation}

where $\theta(k)$ is the measured disk angle, $\hat{\theta}(k)$ is the model output, and $N$ is the number of evaluated samples.

\section{Artificial Neural Network Modelling}

\subsection{Method}

The ANN part of the assignment required at least one simple ANN model and one more advanced ANN architecture \cite{assignment5sc28}.
Therefore, three models were compared: a simple feedforward NARX ANN, a recurrent GRU, and a recurrent LSTM.
The simple NARX model predicts the next angle from delayed voltages and delayed measured angles:

\begin{equation}
\hat{\theta}(k) =
f_{\mathrm{ANN}}(u(k-15), \ldots, u(k-1),
\theta(k-15), \ldots, \theta(k-1)).
\end{equation}

The NARX network has one hidden layer with 50 neurons and follows the practical-session structure \cite{course5sc28}.
This model is easy to interpret and gives a useful baseline.
The GRU and LSTM models use the same sequence input:

\begin{equation}
[(u(k-H),\theta(k-H)), \ldots, (u(k-1),\theta(k-1))]
\rightarrow \hat{\theta}(k).
\end{equation}

with history length $H=15$.
The difference between GRU and LSTM is the recurrent layer: GRU is simpler, while LSTM has a more detailed memory mechanism \cite{course5sc28,cho2014gru,hochreiter1997lstm}.
All models were trained using normalized data, mean squared error loss, and the Adam optimizer \cite{kingma2014adam}.
The known data were split into 70 percent training, 15 percent validation, and 15 percent test data.

\begin{table}[htbp]
\caption{LSTM tuning results on the held-out test split.}
\centering
\begin{tabular}{cccc}
\toprule
History & Hidden size & Prediction RMSE & Simulation RMSE \\
\midrule
10 & 20 & 0.458 deg & 2.433 deg \\
15 & 20 & 0.394 deg & 2.227 deg \\
15 & 40 & 0.267 deg & 2.384 deg \\
15 & 60 & 0.218 deg & 1.771 deg \\
\bottomrule
\end{tabular}
\label{tab:lstm_tuning}
\end{table}

Table~\ref{tab:lstm_tuning} shows the LSTM tuning runs.
The setting with history length 15 and hidden size 60 was selected because it gave the best simulation result among the tested LSTM settings.

\subsection{ANN Results}

Table~\ref{tab:ann_results} compares the simple NARX ANN, the advanced GRU ANN, and the advanced LSTM ANN.
The errors are reported in both radians and degrees in the project files; the table below uses degrees because they are easier to interpret.

\begin{table}[htbp]
\caption{ANN validation results on the held-out test split.}
\centering
\begin{tabular}{lcc}
\toprule
Model & Prediction RMSE & Simulation RMSE \\
\midrule
Simple NARX ANN & 2.442 deg & 16.130 deg \\
Advanced GRU ANN & 0.202 deg & 2.031 deg \\
Advanced LSTM ANN & 0.235 deg & 1.893 deg \\
\bottomrule
\end{tabular}
\label{tab:ann_results}
\end{table}

Fig.~\ref{fig:ann_errors} shows the same comparison visually.
The left plot uses a logarithmic scale because the NARX simulation error is much larger than the recurrent-model errors.
The right plot only compares GRU and LSTM, so the smaller difference between these two models is easier to see.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\columnwidth]{figures/ann_three_model_comparison.png}}
\caption{Prediction and simulation RMSE for the three ANN models.}
\label{fig:ann_errors}
\end{figure}

Both recurrent models clearly performed better than the simple NARX ANN.
The GRU gave the best prediction result, while the LSTM gave the best simulation result.
This is reasonable because prediction is a one-step task using measured past angles, while simulation feeds the model's own predictions back into the input.
The simpler GRU can fit the one-step relation very well, but the LSTM memory mechanism appears to be more stable over a longer simulated trajectory.

Fig.~\ref{fig:ann_prediction} and Fig.~\ref{fig:ann_simulation} show the prediction and simulation errors for the recurrent models.
These plots are used instead of only showing the angle trajectories, because the GRU and LSTM trajectories are very close to the measured angle and are hard to compare by eye.

\begin{figure}[htbp]
\centerline{\includegraphics[width=\columnwidth]{figures/ann_prediction_error_zoom.png}}
\caption{Prediction error for the GRU and LSTM models.}
\label{fig:ann_prediction}
\end{figure}

\begin{figure}[htbp]
\centerline{\includegraphics[width=\columnwidth]{figures/ann_simulation_error_zoom.png}}
\caption{Simulation error for the GRU and LSTM models.}
\label{fig:ann_simulation}
\end{figure}

\subsection{ANN Discussion}

The GRU was added to check whether the extra complexity of an LSTM was really needed.
This gives a useful comparison: GRU is slightly better for prediction, but LSTM is better for simulation.
Since simulation is the more difficult task for a dynamics model, the tuned LSTM output files were selected as the final ANN submission files.
The main limitation is that only a small set of recurrent hyperparameters was tested, so a better setting may still exist.

\section{Gaussian Process Modelling}

This section should be completed by the GP modelling group member.
It should describe the GP model structure, the selected kernel, the hyperparameters, and the final prediction and simulation results.
The section should also explain why the chosen GP structure is suitable for modelling the unbalanced disk dynamics.

\subsection{GP Model Structure}

TODO: Explain the chosen GP input-output structure.
For example, state whether the model is NARX, NOE, or another structure.
Also mention the input delays, output delays, and whether the model predicts the angle directly or predicts a change in angle.

\subsection{Kernel and Hyperparameters}

TODO: Explain the kernel choice and the hyperparameters.
Useful theory points are the GP mean function, covariance function, marginal likelihood, and hyperparameter optimization.
If sparse GP was used, explain why it was needed and what approximation was made.

\subsection{GP Results}

TODO: Add the GP prediction and simulation RMSE values.
Also add the final GP prediction and simulation output files that will be included in the submission zip.
Include one GP validation plot if space allows.

\section{Policy Learning}

This section should be completed by the control group member.
It should describe the learned policies used to swing up the disk and keep it near the upright position.

\subsection{Swing-Up Objective}

TODO: Explain the control goal.
The controller should move the disk from the stable bottom position to the unstable upright position and keep it there.
Mention the voltage limit and how success is measured.

\subsection{Q-Learning or DQN Controller}

TODO: Explain the Q-learning/DQN controller.
Include the state, action, reward function, exploration method, training setup, and final simulation result.
Useful theory points are the Markov decision process, Q-function, reward, and exploration-exploitation trade-off.
If a DQN was used, cite the DQN reference.

\subsection{Model Internalization or Actor-Critic Controller}

TODO: Explain the second control method.
Describe how it differs from the Q-learning/DQN method and why it was tested.
If an actor-critic or policy-gradient idea was used, cite the policy-gradient reference.

\subsection{Swing-Up Results}

TODO: Add a table with the final swing-up results.
Useful metrics are success rate, mean absolute angle error near upright, and control effort.
Also include one angle plot and one voltage plot if space allows.

\section{Reference Tracking}

TODO: Explain how the controller was extended from swing-up to reference tracking.
The report should describe the reference signal, the reward function, and how one policy handles both swing-up and tracking.

\subsection{Reference Tracking Results}

TODO: Add the final tracking result table.
Useful metrics are tracking RMSE, success rate, and tracking ratio.
Include a plot with the reference angle and the measured/simulated disk angle.

\section{Real Setup Experiments}

This section should be filled in after the real setup tests.
If the tests are not successful, still describe what happened and why.
The discussion of practical limitations is useful for the final grade.

\subsection{Test Plan}

TODO: Record the date, controller version, number of trials, initial condition, safety checks, and logged signals.

\subsection{Experimental Results}

TODO: Add the real setup results.
Compare the real setup behaviour with the simulation behaviour.
Mention possible causes of differences, such as friction, sensor noise, delay, saturation, or model mismatch.

\section{Discussion}

This section should compare the main choices made in the project.
It should not only repeat the results, but also explain what was learned from them.

\subsection{Model Comparison}

TODO: Compare GP and ANN models.
Discuss prediction accuracy, simulation accuracy, interpretability, training effort, and limitations.

\subsection{Controller Comparison}

TODO: Compare the control methods.
Discuss robustness, tuning difficulty, success rate, and simulation-to-real transfer.

\subsection{Limitations}

TODO: Discuss limitations honestly.
Possible points are noisy measurements, limited hyperparameter tuning, hidden test scores not being known during development, and limited time on the real setup.

\section{Conclusion}

TODO: Summarize the final project results in one short section.
Mention the best modelling result, the best control result, and the outcome of the real setup test if available.
Also state what the group would improve with more time.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
